# Copy to .env and fill values. Used by connect.php via environment variables (recommended).
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=emergency_application
DB_USER=root
DB_PASS=

# Development only: include OTP in response from send_otp.php
RETURN_OTP=true
